[project]
name = "multilayer-dmp-calculations"
version = "1.0.0"
description = "Calculation-only reproduction code for the revised manuscript"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.4",
    "numpy>=2.2",
    "scipy>=1.15",
]
